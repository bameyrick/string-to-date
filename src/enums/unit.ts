import { Units } from '../models';

export const Unit: Record<Units, Units> = {
  year: 'year',
  month: 'month',
  week: 'week',
  day: 'day',
  hour: 'hour',
  minute: 'minute',
  second: 'second',
  millisecond: 'millisecond',
  offset: 'offset',
};
